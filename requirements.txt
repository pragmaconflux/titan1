# Core dependencies
# Core dependencies (required)
# All core functionality uses only Python stdlib for maximum portability

# Optional dependencies for enhanced features
# Install with: pip install titan-decoder[all]

# For forensics and system monitoring
psutil>=5.9.0  # Memory monitoring, process info (used in resource_manager, device_forensics)

# For enrichment features
geoip2>=4.7.0  # GeoIP lookups (requires MaxMind GeoLite2 database)
python-whois>=0.8.0  # WHOIS lookups for domains/IPs
yara-python>=4.3.0  # YARA rule scanning
requests>=2.31.0  # HTTP requests for VirusTotal API

# For testing
pytest>=7.4.0  # Test framework
pytest-cov>=4.1.0  # Coverage reporting

# Development tools
black>=23.0.0  # Code formatting
flake8>=6.0.0  # Linting
mypy>=1.5.0  # Type checking